import 'package:flutter/material.dart';

import 'user_app_shell_screen.dart';

class UserCommandCenterScreen extends StatelessWidget {
  const UserCommandCenterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const UserAppShellScreen();
  }
}
